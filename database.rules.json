{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$roomId": {
        ".read": "$roomId.matches(/^[0-9]{5}$/)",
        ".write": "$roomId.matches(/^[0-9]{5}$/)",
        "roomId": {
          ".validate": "newData.isString() && newData.val() === $roomId"
        },
        "gameState": {
          ".validate": "!newData.exists() || newData.val() === 'Lobby' || newData.val() === 'Playing' || newData.val() === 'GameOver'"
        },
        "settings": {
          ".validate": "!newData.exists() || newData.hasChildren(['teamCount', 'roundTimerSeconds', 'lossCardCount', 'wordCategory', 'extraRows'])",
          "teamCount": {
            ".validate": "newData.val() === 2 || newData.val() === 3 || newData.val() === 4"
          },
          "roundTimerSeconds": {
            ".validate": "newData.isNumber() && newData.val() >= 15 && newData.val() <= 600"
          },
          "lossCardCount": {
            ".validate": "newData.val() === 1 || newData.val() === 2 || newData.val() === 3 || newData.val() === 4"
          },
          "wordCategory": {
            ".validate": "newData.isString() && (newData.val() === 'General' || newData.val() === 'Cities' || newData.val() === 'Animals')"
          },
          "extraRows": {
            ".validate": "newData.isNumber() && (newData.val() === 0 || newData.val() === 1 || newData.val() === 2 || newData.val() === 3)"
          }
        },
        "currentTurn": {
          ".validate": "!newData.exists() || newData.val() === 'Red' || newData.val() === 'Blue' || newData.val() === 'Green' || newData.val() === 'Gold'"
        },
        "turnPhase": {
          ".validate": "!newData.exists() || newData.val() === 'Clue' || newData.val() === 'Guess'"
        },
        "isPaused": {
          ".validate": "!newData.exists() || newData.isBoolean()"
        },
        "pausedRemainingMs": {
          ".validate": "!newData.exists() || newData.isNumber() || newData.val() === null"
        },
        "pendingRevealCardId": {
          ".validate": "!newData.exists() || newData.isNumber() || newData.val() === null"
        },
        "pendingRevealAt": {
          ".validate": "!newData.exists() || newData.isNumber() || newData.val() === null"
        },
        "presence": {
          "$playerId": {
            ".validate": "!newData.exists() || newData.isBoolean()"
          }
        },
        "operativeSelections": {
          "$playerId": {
            ".validate": "!newData.exists() || newData.isNumber() || newData.val() === null"
          }
        },
        "turnEndsAt": {
          ".validate": "!newData.exists() || newData.isNumber()"
        },
        "eliminatedTeams": {
          "$teamIndex": {
            ".validate": "!newData.exists() || newData.val() === 'Red' || newData.val() === 'Blue' || newData.val() === 'Green' || newData.val() === 'Gold'"
          }
        },
        "winner": {
          ".validate": "!newData.exists() || newData.val() === 'Red' || newData.val() === 'Blue' || newData.val() === 'Green' || newData.val() === 'Gold'"
        },
        "players": {
          "$playerIndex": {
            ".validate": "!newData.exists() || (newData.hasChildren(['id', 'name', 'team', 'role', 'isHost']) && newData.child('id').isString() && newData.child('id').val().length > 0 && newData.child('name').isString() && newData.child('name').val().length >= 2 && newData.child('name').val().length <= 24 && (newData.child('team').val() === 'Red' || newData.child('team').val() === 'Blue' || newData.child('team').val() === 'Green' || newData.child('team').val() === 'Gold' || newData.child('team').val() === 'Unassigned') && (newData.child('role').val() === 'Spymaster' || newData.child('role').val() === 'Operative') && newData.child('isHost').isBoolean())"
          }
        },
        "board": {
          "$cardIndex": {
            ".validate": "!newData.exists() || (newData.hasChildren(['id', 'text', 'type', 'isRevealed']) && newData.child('id').exists() && newData.child('text').isString() && newData.child('text').val().length >= 1 && newData.child('text').val().length <= 20 && (newData.child('type').val() === 'Red' || newData.child('type').val() === 'Blue' || newData.child('type').val() === 'Green' || newData.child('type').val() === 'Gold' || newData.child('type').val() === 'Neutral' || newData.child('type').val() === 'Control') && newData.child('isRevealed').isBoolean())"
          }
        },
        "clues": {
          "$clueIndex": {
            ".validate": "!newData.exists() || (newData.hasChildren(['team', 'text', 'count', 'createdAt']) && (newData.child('team').val() === 'Red' || newData.child('team').val() === 'Blue' || newData.child('team').val() === 'Green' || newData.child('team').val() === 'Gold') && newData.child('text').isString() && newData.child('text').val().length >= 1 && newData.child('text').val().length <= 24 && newData.child('count').isNumber() && newData.child('count').val() >= 1 && newData.child('count').val() <= 9 && newData.child('createdAt').isNumber())"
          }
        }
      }
    }
  }
}
